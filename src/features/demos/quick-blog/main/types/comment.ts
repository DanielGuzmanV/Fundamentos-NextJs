// Tipos para la API de JSONPlaceholder
// Comentario original de JSONPlaceholder
export interface JsonPlaceholderComment {
  postId: number;
  id: number;
  name: string;
  email: string;
  body: string;
}
